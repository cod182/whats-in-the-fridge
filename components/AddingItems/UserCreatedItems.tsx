import React from 'react'
type Props = {
  selectedArea: selectionProps;
  availableItems: availableItem[]
}
const UserCreatedItems = ({ selectedArea, availableItems }: Props) => {
  return (
    <div>UserCreatedItems</div>
  )
}

export default UserCreatedItems