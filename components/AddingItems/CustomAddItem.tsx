import React from 'react'
type Props = {
  selectedArea: selectionProps;
  availableItems: availableItem[]
  userId: string;
}
const CustomAddItem = ({ selectedArea, availableItems, userId }: Props) => {
  return (
    <div>CustomAddItem</div>
  )
}

export default CustomAddItem